# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. **Task 1 — Extend note dict**: Add a 	ags key (list of strings, default empty) to the note dict in app.py. _Verify: existing tests still pass; a new note has tags == []._
2. **Task 2 — Update new-note form**: Add a comma-separated tags input field to new_note.html and parse it in the POST handler. _Verify: creating a note with 'work, urgent' yields note["tags"] == ["work", "urgent"]._
3. **Task 3 — Display tags as chips**: Show tags as chips next to each note title in the list view. _Verify: a note with tags shows colored chip elements in the browser._
4. **Task 4 — Build filter bar**: Add an All/tag filter bar at the top of the notes list. _Verify: clicking a tag filters the list to only notes with that tag._
5. **Task 5 — Wire filter logic in app.py**: Add route logic to filter notes by tag query param. _Verify: /notes?tag=work returns only notes tagged work._
6. **Task 6 — Write tag parsing tests**: Add tests for comma-separated tag parsing (empty string, whitespace, duplicates). _Verify: pytest passes all new tests._
7. **Task 7 — Write filter behavior tests**: Add tests that the filter route returns only matching notes. _Verify: pytest passes all new tests._
8. **Task 8 — Regression check**: Run full pytest suite. _Verify: all existing and new tests pass._

## Task 1 context brief
Goal: Add a tags key to the note dict in app.py so every note has a tags field (list of strings, default empty list).

Constraints:
- Notes are stored as dicts in an in-memory list — no database.
- Do not change the title or body fields.
- Tags default to an empty list if not provided.
- Do not touch the templates yet.

Files expected to change:
- app.py only.

Acceptance:
- A note created without tags has tags == [].
- Existing tests still pass.
- No template changes in this task.

Plan mode first — propose the change before writing any code.

## Task 1 — what happened
- Claude's plan summary: Added tags key with default [] to the note dict in app.py's new_note route.
- What I pushed back on: Nothing — the change was minimal and correct.
- Verification result: Existing tests passed, new notes have tags == [].

## Task 2 — what happened
- Claude's plan summary: Added a tags input field to new_note.html and parsed comma-separated input into a list in the POST handler.
- What I pushed back on: Confirmed Claude stripped whitespace from each tag.
- Verification result: Creating a note with 'work, urgent' yielded ["work", "urgent"].

## Task 3 — what happened
- Claude's plan summary: Added chip-style spans for each tag next to note titles in the list template.
- What I pushed back on: Nothing — rendered correctly in browser.
- Verification result: Tags displayed as chips next to note titles.

## Reflection — where I redirected
- Task 2 required pushing back to confirm whitespace stripping, which wasn't in Claude's first plan summary.
- The brief for task 2 was tighter because task 1 showed Claude stays in scope when files are explicitly listed.
- If redoing this from scratch I would have included whitespace/edge case handling as an explicit constraint in the task 2 brief from the start.
